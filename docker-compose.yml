services:
  backend:
    build:
      context: ./CanalDenuncia_Back
      dockerfile: Dockerfile
    container_name: canal-denuncias-backend-prod
    ports:
      - "${COMBATEASSEDIO_BACK_PORT}:8080"
    env_file:
      - .env
    environment:
      - JWT_SECURE=true
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - canalDenuncia-network-prod
    volumes:
      - ./uploads:/uploads

  frontend:
    build:
      context: ./CanalDenuncia_Front
      dockerfile: Dockerfile
    container_name: canal-denuncias-frontend-prod
    ports:
      - "${COMBATEASSEDIO_FRONT_PORT}:3000"
    env_file:
      - .env
    environment:
      - API_URL=http://backend:8080
    depends_on:
      - backend
    networks:
      - canalDenuncia-network-prod

volumes:
  canal-denuncias-db-data-prod:

networks:
  canalDenuncia-network-prod:
    driver: bridge
